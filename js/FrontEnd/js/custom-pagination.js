 $.fn.extend({
	pagination:function(content){

		 $("#storyContent").html(content);
	}

});


/* $('#custom-pager').bootpag({
            total: txt.length,
			first: 'first',
			last: 'last'
        }).on("page", function(event, num){
             $("#storyContent").html(txt[num-1]);
        }); */